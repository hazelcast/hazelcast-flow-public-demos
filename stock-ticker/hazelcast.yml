hazelcast:
  cluster-name: flow
  jet:
    enabled: true
  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: true
        member-list:
          - stock-ticker-hazelcast-flow-1:5701
